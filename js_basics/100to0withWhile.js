let num = 100;
while (num >= 0) {
  console.log(num);

  // num = num - 1;
  // The line below does exactly the
  // same thing as the line above.
  num -= 1;
  // This is called "syntax sugar".
  // This is an expression that is often shorter or
  // more readable than another, but they do
  // exactly the same thing.

  // num += 10
  // num *= 7
  // num %= 90
  // num **= 4

  // num--;
  // --num;
}
